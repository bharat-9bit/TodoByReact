import React from 'react';

const Header: React.FC = () => {
  return (
    <div>
      <h1 className="head">TODO List Demo App</h1>
      <span className="para">Do it now.</span>
    </div>
  );
};

export default Header;